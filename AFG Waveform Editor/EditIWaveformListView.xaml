<Window x:Class="Views.EditWaveformListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Views"
        mc:Ignorable="d"
        Title="Waveform List Data"  SizeToContent="WidthAndHeight">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Width="100">Duration(s)</TextBlock>
            <TextBox Width="60" Text="{Binding WaveformEditItem.Duration}" Margin="5,5,5,5" Background="LightBlue"></TextBox>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Width="100">Voltage(V)</TextBlock>
            <TextBox Width="60" Text="{Binding WaveformEditItem.Voltage}" Margin="5,5,5,5" Background="LightBlue"></TextBox>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Click="OnOkClicked" Margin="5,5,5,5">OK</Button>
        </StackPanel>
    </StackPanel>
</Window>
